#include <iostream>
using namespace std;

int main() {

   int number = 0;
   int a;
   
   cin >> number; 


if (number > 0)
{

   while (number > 0)
   {
      a = number % 2;	   	   	   	   
      number = number / 2;
      cout << a;
   }

   cout << endl;
}

   return 0;
}
